//document.querySelectorAll('.btn-outline-secondary').forEach(button => {
    //button.addEventListener('click', function() {
        // Hapus kelas aktif dari tombol lain
       // document.querySelectorAll('.btn-sage').forEach(b => {
           // b.classList.replace('btn-sage', 'btn-outline-secondary');
           // b.classList.remove('text-white');
       // });
        
        // Tambahkan kelas aktif ke yang diklik
       // this.classList.replace('btn-outline-secondary', 'btn-sage');
       // this.classList.add('text-white');
    //});
//});

const selectElement = document.getElementById('navigator');
selectElement.addEventListener('change', (event) =>
{
    const targetId = event.target.value;

});